package Server;

import javax.websocket.server.ServerEndpointConfig.Configurator;

public class ChatConfig extends Configurator {
    // This class can be left empty if no special configuration is needed.
}